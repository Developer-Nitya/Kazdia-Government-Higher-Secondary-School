# Logo and Full Background View Fix Notes

- Increased hero logo size and added safe inner padding to prevent top/bottom clipping.
- Switched hero background layers to `background-size: contain` so each slider image remains fully visible.
- Kept the fade slider behavior unchanged.
- Bumped CSS asset version to refresh browser cache.
